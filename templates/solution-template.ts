/**
 * {PROBLEM_ID}. {PROBLEM_TITLE}
 * 
 * {SHORT_DESCRIPTION}
 * 
 * @param {PARAM_TYPE} {PARAM_NAME} - {PARAM_DESCRIPTION}
 * @returns {RETURN_TYPE} - {RETURN_DESCRIPTION}
 */
function {FUNCTION_NAME}({FUNCTION_PARAMS}): {RETURN_TYPE} {
    // Escreva sua solução aqui
    
    // Código de exemplo:
    // return result;
}

// Exemplos de teste
function runTests(): void {
    // Exemplo 1
    const example1 = {EXAMPLE_INPUT_1};
    console.log(`Entrada: ${JSON.stringify(example1)}`);
    console.log(`Saída: ${JSON.stringify({FUNCTION_NAME}(example1))}`);
    console.log(`Saída esperada: ${JSON.stringify({EXAMPLE_OUTPUT_1})}`);
    console.log('---');
    
    // Exemplo 2
    // const example2 = {EXAMPLE_INPUT_2};
    // console.log(`Entrada: ${JSON.stringify(example2)}`);
    // console.log(`Saída: ${JSON.stringify({FUNCTION_NAME}(example2))}`);
    // console.log(`Saída esperada: ${JSON.stringify({EXAMPLE_OUTPUT_2})}`);
    // console.log('---');
}

// Executar os testes
runTests(); 